<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- YEH UPDATE HUA HAI (Username ko check kar rahe hain) -->
    <title><%= pageTitle %> <% if (username) { %>- (<%= username %>)<% } %> | Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { 'sans': ['Inter', 'sans-serif'], },
                    colors: {
                        'brand-green': '#16a34a',
                        'esg-dark': '#1a1a1a', 
                        'job-blue': '#1d4ed8',
                        'job-yellow': '#ca8a04',
                    }
                }
            }
        }
    </script>
    <style>
        body { font-family: 'Inter', sans-serif; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
    </style>
</head>
<body class="bg-gray-100 text-esg-dark">

    <nav class="bg-white shadow-md">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex-shrink-0 flex items-center">
                    <h1 class="text-2xl font-extrabold text-esg-dark">Sustain<span class="text-brand-green">Wire</span> Admin</h1>
                </div>
                <!-- YEH UPDATE HUA HAI (Welcome message add kiya hai) -->
                <div class="flex space-x-4 items-center">
                    <% if (username) { %>
                        <span class="text-gray-500 text-sm hidden sm:block">Welcome, <strong><%= username %></strong></span>
                    <% } %>
                    <a href="/admin/dashboard" class="text-gray-900 hover:text-brand-green font-medium transition-colors">Dashboard</a>
                    <a href="/" class="text-gray-500 hover:text-brand-green font-medium transition-colors" target="_blank">View Site</a>
                    <a href="/logout" class="text-red-500 hover:text-red-700 font-medium transition-colors">Logout &rarr;</a>
                </div>
            </div>
        </div>
    </nav>