<%- include('../partials/admin-header') %>

<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <h1 class="text-3xl font-bold text-esg-dark mb-8">Edit Special Report (Hero Section)</h1>

    <!-- Form -->
    <!-- Yeh form hamesha /admin/special-report/edit par submit hoga -->
    <form action="/admin/special-report/edit" method="POST" enctype="multipart/form-data" class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">

        <!-- Title -->
        <div class="mb-4">
            <label for="title" class="block text-sm font-bold text-gray-700 mb-2">Title</label>
            <!-- 'value' attribute mein purana data dikhayein -->
            <input type="text" id="title" name="title" value="<%= report.title %>" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-green" required>
        </div>

        <!-- Image Upload -->
        <div class="mb-4">
            <label for="imageUrl" class="block text-sm font-bold text-gray-700 mb-2">Change Featured Image (Optional)</label>
            <p class="text-sm text-gray-500 mb-2">Current Image:</p>
            <img src="<%= report.imageUrl %>" alt="<%= report.title %>" class="h-24 w-32 object-cover rounded mb-2">
            <input type="file" id="imageUrl" name="imageUrl" class="w-full px-3 py-2 border border-gray-300 rounded-lg">
            <p class="text-sm text-gray-500 mt-1">Leave blank to keep the current image.</p>
        </div>

        <!-- Content (Description) -->
        <div class="mb-6">
            <label for="content" class="block text-sm font-bold text-gray-700 mb-2">Content (Description)</label>
            <!-- Textarea ke beech mein purana data daalein -->
            <textarea id="content" name="content" rows="6" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-green" required><%= report.content %></textarea>
        </div>

        <!-- Submit Button -->
        <div>
            <button type="submit" class="w-full bg-brand-green text-white font-bold py-3 px-6 rounded-lg hover:bg-green-700 transition-colors">
                Update Special Report
            </button>
        </div>

    </form>
</div>

</body>
</html>